import {DefaultTheme} from 'react-native-paper';

export const buttonTheme = {
    ...DefaultTheme,
    roundness: 0,
    colors: {
        ...DefaultTheme.colors,
        primary: '#FF8E42',
        accent: 'black',
    },
};
